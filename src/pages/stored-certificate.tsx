import Layout from "../components/Layout";
import StoredCertificate from "../components/StoredCertificate";

const StoredCertificatePage: React.FC = () => {
  return (
    <Layout>
      <StoredCertificate />
    </Layout>
  );
};

export default StoredCertificatePage;
